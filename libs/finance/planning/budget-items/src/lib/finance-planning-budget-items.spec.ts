import { financePlanningBudgetItems } from './finance-planning-budget-items';

describe('financePlanningBudgetItems', () => {
  it('should work', () => {
    expect(financePlanningBudgetItems()).toEqual(
      'finance-planning-budget-items'
    );
  });
});
